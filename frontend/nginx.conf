server {
    listen 80;

    location /api/ {
        proxy_pass http://prolog-backend:8080/api/;
        proxy_set_header Host $host;
    }

    location / {
        root /usr/share/nginx/html;
        index index.html;
    }
}